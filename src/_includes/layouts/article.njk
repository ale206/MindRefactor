---
layout: base.njk
---
{% from "macros/tagcloud.njk" import tagCloud %}

<div class="post-list-page blog-split-layout">

  <article class="featured-post">
    <header class="featured-header article-header">
      <div class="article-meta">
        <time datetime="{{ date | isoDate }}">{{ date | readableDate("en-GB") }}</time>
      </div>

      <h1 class="article-title">{{ title }}</h1>

      {% if description %}
      <p class="article-description">{{ description }}</p>
      {% endif %}

      {# ── Article tags ── #}
      {% if postTags and postTags.length %}
      <div class="article-tags">
        {% for t in postTags %}
        <a href="{{ ('/en/tags/' + t + '/') | url }}" class="tag-pill">{{ t }}</a>
        {% endfor %}
      </div>
      {% endif %}
    </header>

    <div class="article-body">
      {{ content | safe }}
    </div>

    <footer class="article-footer">
      <a href="{{ '/en/' | url }}" class="back-link">← All articles</a>
    </footer>
  </article>

  <aside class="sidebar-posts">
    {# ── Tag cloud ── #}
    {{ tagCloud(collections.tagListEn, "en") }}

    <h3 class="sidebar-title" style="margin-top:1.75rem;">Recent articles</h3>
    <ol class="post-list" reversed>
      {% for post in collections.postsEn | excludeAndLimit(page.url, 5) %}
        <li class="post-card">
          <time class="post-card-date" datetime="{{ post.date | isoDate }}">
            {{ post.date | readableDate("en-GB") }}
          </time>
          <a href="{{ post.url | url }}" class="post-card-title">{{ post.data.title }}</a>
        </li>
      {% endfor %}
    </ol>
    <a href="{{ '/en/articles/' | url }}" class="sidebar-all-link">All articles →</a>
  </aside>

</div>
